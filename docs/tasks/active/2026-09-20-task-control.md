# Issue 编码与本机人工验收

状态：实现与运行切换完成；人工观感待确认。负责人：Codex。更新时间：2026-09-20 UTC。

## 目标与验收

落实用户确认的本地工作流：程序检查、最多一次修复、持久化停止、可靠草稿交付、按需同提交人工实例。不限制总时间或 token；不接 Actions，不合并、不部署业务服务。

## 进度与决定

- [x] 工作区外任务计划和状态、适配器检查/交付控制。
- [x] 按业务选择的宿主验收、合成数据和依赖复用。
- [x] 故障回归、隔离 DeepSeek 实跑、本机实例验证。
- [x] 空闲时更新 Symphony，保留回退点。

隔离实现工作树：`.local/task-control-worktree`，基线 `c4b4879`。当前运行服务不读取该目录。任务计划由需求准备者在模型工作区外登记；实现模型不能延长检查轮次或修改验收计划。

## 验证证据

隔离工作树与集成后的主工作区 quick 均通过；最终报告及源码指纹统一登记在 `.local/symphony/task-control-rollout-20260920/validation.json`。DeepSeek 实跑已创建草稿 PR 12；一次检查通过、5 次模型工具调用。PR 10 固定 SHA 的 64 项 UI、真实登录和预览停止通过。具体范围及失败修复见 [验证记录](../../06-validation.md)。

## 交接与恢复

实现已集成主工作区，保留同期文档修改。本次 PR 10 验收实例已停止；日常 Demo 未改，Symphony 已切换并确认空闲、模块哈希及中文页面健康。旧容器与回退脚本保留在 `.local/symphony/task-control-rollout-20260920/` 对应清单。需人工审查 PR 12 时另行准备其完整 SHA，不复用 PR 10 的批准。

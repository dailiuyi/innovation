# GH-9 按路径发布与草稿交接

状态：完成（草稿交付，业务验收待完成）。负责人：Codex。更新时间：2026-09-20 UTC。

## 目标与验收

停止 GH-9 的低效发布会话，保留工作区和 PR #10；增加不经过模型搬运正文的按路径发布工具，补交两个缺失文件，将宿主验收明确交接。

## 进度与决定

- 已移除 ready，调度运行/重试/阻塞队列为空，工作区保留；未重启 Symphony 或 Demo。
- 新增 `symphony_publish.py`，适配器注入 `github_publish_files`，底层沿用 tracker 的认证工具；入口限制与失败处理见 [运行手册](../../15-symphony.md)。启动脚本保留只读挂载。
- 草稿 PR #10 补交提交 `5b8ff6636f60472771777e5bc8fc5b75376f7157`，13 个变更文件全部逐文件哈希比对通过。PR 说明移除“正在补推”并列出宿主待验收项；Issue 为 review。

## 验证证据

宿主系统工具 HEAD 基于 `5806c68680212dccc3874b94a0ddd49dde080a5a`，保留原有未提交改动。Linux 文件发布 12 项、路由协议 18 项通过。Windows 符号链接测试受权限限制，Linux 已实际覆盖。发布使用宿主 gh 传输完成，适配器与 tracker 转发由协议测试覆盖，未为验收工具另开真实模型会话。

`.local/symphony/publish-fix-20260920/` 保存旧适配器/工作流、GH-9 补丁、请求/收据、前后 PR 状态、远端树及文件哈希核对。发布补交不改变 GH-9 源码字节，不复用旧报告冒充新业务验收。

## 交接与恢复

GH-9 工作区和草稿 PR 保留，自动运行已结束。宿主真实后端、Windows ingestion、三视口人工检查、独立审查及 preview 待完成。系统工具修改保留宿主工作区，未混入 GH-9 PR。只有明确返工时才重新加 ready。

发布工具暂不支持删除、符号链接及超过限制的文件，遇到时保存补丁并交接。超时后先查远端，禁止盲目重复发布。
